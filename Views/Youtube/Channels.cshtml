@model List<Dashboard.Models.youtube.ChannelYoutube>
@{
    ViewData["Title"] = "Channels";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card-columns">
    @foreach (var channel in Model)
    {
        @Html.Partial("_ChannelCard", channel)
    }
</div>

